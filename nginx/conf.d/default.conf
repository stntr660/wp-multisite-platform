# AirArom WordPress Site
server {
    listen 80;
    server_name airarom.ma www.airarom.ma;
    
    location / {
        proxy_pass http://airarom:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # Rate limiting
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# ElectroRomanos WordPress Site
server {
    listen 80;
    server_name electroromanos.ma www.electroromanos.ma;
    
    location / {
        proxy_pass http://electroromanos:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    # Rate limiting
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# FreshExpress WordPress Site
server {
    listen 80;
    server_name freshexpress.ma www.freshexpress.ma;
    
    location / {
        proxy_pass http://freshexpress:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# OumniaRentalCars Static Site
server {
    listen 80;
    server_name oumniarentalcars.com www.oumniarentalcars.com;
    
    location / {
        proxy_pass http://oumniarentalcars:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# Sabeel Agency WordPress Site
server {
    listen 80;
    server_name sabeel.agency www.sabeel.agency;
    
    location / {
        proxy_pass http://sabeel:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# Sabeel Academy WordPress Site
server {
    listen 80;
    server_name sabeelacademy.ma www.sabeelacademy.ma;
    
    location / {
        proxy_pass http://sabeelacademy:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# Sumo WordPress Site
server {
    listen 80;
    server_name sumo.ma www.sumo.ma;
    
    location / {
        proxy_pass http://sumo:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# YvesMorel WordPress Site
server {
    listen 80;
    server_name yvesmorel.ma www.yvesmorel.ma;
    
    location / {
        proxy_pass http://yvesmorel:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}

# Zonemation WordPress Site
server {
    listen 80;
    server_name zonemation.com www.zonemation.com;
    
    location / {
        proxy_pass http://zonemation:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_connect_timeout 30s;
        proxy_send_timeout 30s;
        proxy_read_timeout 30s;
    }
    
    limit_req zone=one burst=10 nodelay;
    limit_conn addr 10;
}